#!/bin/sh

# netifd ifup of any interface — WiFi VAPs may come up after net add.
# Retry apply; harmless if rax0 is not ready yet.

[ "$ACTION" = ifup ] || exit 0

. /lib/functions.sh
. /lib/viettel-nr3053-throughwall.sh

viettel_throughwall_supported || exit 0
viettel_throughwall_update

exit 0
